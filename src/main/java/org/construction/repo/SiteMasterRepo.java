package org.construction.repo;

import org.construction.model.SiteMaster;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SiteMasterRepo extends JpaRepository<SiteMaster, Long> {
}
